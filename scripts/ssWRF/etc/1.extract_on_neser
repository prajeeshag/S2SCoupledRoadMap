#!/bin/sh

cd DAT
for file in FORE.???????? SURF.????????; do
  echo $file
  wgrib.ns -s $file > inv
  for ((i=1;i<=10;i++)); do
    grep "Perturbed forecast $i:" < inv | wgrib.ns -i $file -s -grib -o $file.$i > /dev/null
  done
  rm inv
done
cd ..
